apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: aitelemetry-site
spec:
  secretStoreRef:
    name: nerc-cluster-secrets
    kind: ClusterSecretStore
  target:
    name: aitelemetry-site
  dataFrom:
  - extract:
      key: REPLACE_IN_OVERLAY
