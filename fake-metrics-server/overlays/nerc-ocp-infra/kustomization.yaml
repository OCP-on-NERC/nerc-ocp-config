apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: openshift-storage
resources:
  - ../../base/

patches:
  - path: deploy_patch.yaml

configMapGenerator:
  - files:
      - metrics.json
    name: fake-metrics
