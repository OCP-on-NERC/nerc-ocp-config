apiVersion: k8s.cni.cncf.io/v1
kind: NetworkAttachmentDefinition
metadata:
  name: daemon-network
spec:
  config: |-
    {
      "cniVersion": "0.3.1",
      "type": "host-device",
      "name": "daemon-network",
      "device": "ibm-scale0"
    }